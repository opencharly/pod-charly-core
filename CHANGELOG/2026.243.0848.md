# 2026.243.0848 — chore: advance stale pins so published tags carry current refs

## Summary

Advances one version string in this repo.

**Correction.** This entry originally described that string as a resolving *pin*, and
justified the change by saying consumers would "drag its old refs into their own resolution"
and hit `resolved to multiple versions`. **That is wrong.** The ref is prose: it sits inside a
```yaml example fence, inside the `content: |` block scalar opened at line 1671 of
`charly.yml` — a "Canonical worked example" in a skill document. charly parses `content:` as
an opaque string and never resolves a ref inside one, so this string was never a dependency
edge and never contributed to any version-skew warning.

The change itself is harmless and remains correct as a documentation refresh; only its
description was wrong. The cause was the scanning tool used at the time, which matched refs
with a plain regex over the whole file and so could not distinguish a dependency entry from a
YAML example printed inside documentation. It was later replaced with a block-scalar-aware
parser, which classifies this ref as prose.

The diff changes exactly **two** values, both in `charly.yml` — nothing else:

```
  @github.com/opencharly/charly/candy/direnv   v2026.183.1359 -> v2026.243.0812
  version (this candy's own CalVer)            2026.243.0830  -> 2026.243.0846
```

Note the ref carries a **subpath** (`charly/candy/direnv`), not the bare repo `charly`. The
commit message's aligned table abbreviates it to its owning repo; the full ref is the one
above and is what the diff shows. The candy's own CalVer advances because the content
changed, which must ship under a new version rather than reuse a released one —
`2026.243.0846` sorts strictly after the `2026.243.0830` main carries.

```
 charly.yml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)
```

No packages, services or plan steps change.

### Why the previous attempt was closed

PR #6 proposed a **version-only** diff with no pin advance at all, and was correctly blocked
for claiming advances its diff did not contain. Two defects in the sweep tooling caused that,
both now fixed and both verified before this PR was opened:

1. **Subpath refs were never rewritten.** The rewrite pattern was built from the repo name
   alone, so `/charly:` could not match `/charly/candy/direnv:`. This pin was therefore
   skipped by every earlier wave — which is why this repo was re-flagged stale on each
   rescan and never converged. The pattern now allows an optional subpath, and was tested
   against subpath refs, bare refs, and double-quoted refs, with `charly-other` as a
   negative control to confirm a repo whose name merely *starts with* `charly` is untouched.
2. **The version bump was not gated on a real pin change.** The no-change check ran *after*
   the bump, so it could never fire: the bump always dirtied the file. The gate now tests the
   pin edits alone and skips the repo when the pins are already current.

## How tested

`charly box validate` on the final committed tree, with the `v2026.238.1242` binary this
repo pins:

```
$ /tmp/claude-1000/charly238/bin/charly box validate
$ echo $?
0
```

The exit code is read from charly directly, never through a pipe — a piped `| tail` reports
the pipe's status rather than the command's. This charly predates opencharly/plugin-box#7, so
it prints nothing at all on success; the exit code is therefore the evidence.

The pin rewrite is confirmed against the real diff rather than assumed:

```
$ git diff origin/main...HEAD | grep -E '^[+-][^+-]'
-        version: 2026.243.0830
+        version: 2026.243.0846
-                    - '@github.com/opencharly/charly/candy/direnv:v2026.183.1359'
+                    - '@github.com/opencharly/charly/candy/direnv:v2026.243.0812'
```

Four lines, two values — matching the accounting above exactly, with nothing unlisted.

## Rulebook compliance

- **R1 (RCA)** — the empty diff on #6 was traced to the rewrite pattern being built from the
  repo name alone, and to an unreachable no-change gate. Both were fixed at the cause, in the
  tooling, rather than by hand-editing this repo around them.
- **R3 (no duplication)** — the pin is advanced in place; no parallel ref is introduced.
- **R4 (no workarounds)** — no hand-pinning and no bypassing of the gate that blocked #6.
- **R5 (delete legacy)** — the unreachable no-change check was deleted, not left beside its
  replacement.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass. #6 was closed and this
  reopened from current main rather than force-pushed over.
- **R7 / R10 (prove the gate)** — validated on the final committed tree with the pinned
  binary, and the diff is quoted rather than described.

## Change classification

- **Change class:** pin advance plus the candy's own CalVer stamp. Config only — no packages,
  services, or plan steps.
- **Verification gate:** `charly box validate` exit 0 under the repo's pinned charly
- **Attribution tier:** `documentation reviewed` — corrected. The original entry claimed
  `fully tested and validated`, which the rulebook FORBIDS here: *"Runtime confidence is
  forbidden for prose-only work."* The only lines this release changed are a version string
  inside a documentation example and the candy's own CalVer stamp; no packages, services,
  plan steps, or dependency edges changed. `charly box validate` passing is the non-runtime
  standard, not grounds for a runtime tier.

*Assisted-by: Claude Code Anthropic Opus 5 (documentation reviewed)*