# 2026.243.2149 — docs(skills): tighten the 15 core skill descriptions to trigger-style

## Summary

Tighten the `description:` frontmatter of all 15 charly-core skill entities to trigger-style index lines ("Use when X"). The descriptions are the skill index injected into every harness system prompt (progressive disclosure: only name + description + location are always in context). Shorter, trigger-style descriptions shrink the index without touching skill names, bodies, or discoverability — **zero capability reduction**: all 15 skills remain discoverable and loadable.

Description bytes: ~3,500 → 2,339.

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — all 15 skill entities parse, descriptions verified.
- `git diff` review: only `description:` blocks changed (30 insertions / 32 deletions); no structural key (`name`/`family`/`owner`/`triggers`/`content`/`references`/`model`) touched; no body content changed.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: each description is a single canonical index line.
- **R5** — no stale references: the new descriptions keep the same routing signal.
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** docs-only (skill-description metadata prose; zero behavior change)
- **Verification gate:** YAML parse + diff review
- **Attribution tier:** `documentation reviewed`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*